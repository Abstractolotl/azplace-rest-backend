debug: true

app:
  cas:
    url: https://login.united-internet.org/ims-sso
    redirectUrl: https://place.azubi.server.lan
  defaultKeyValidTime: 120
  
spring:
  elasticsearch:
    uris: http://elastic.azplace.azubi.server.lan

  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://noucake.ddns.net:3306/aztube
    username: root
    password: rootpw
    driver-class-name: com.mysql.cj.jdbc.Driver

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui

logging:
  level:
    root: INFO

management:
  endpoints:
    enabled-by-default: false # disable all actuator endpoints by default
    jmx:
      exposure:
        include: '*'
    web:
      base-path: /actuator
      exposure:
        include: health

  endpoint: # enable only those endpoints we want
    health:
      enabled: true
      group: # special health endpoints for kubernetes probes
        readyness:
          include: readyness
        liveness:
          include: liveness

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://noucake.ddns.net:3306/aztube
    username: root
    password: rootpw
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE